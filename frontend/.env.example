# API URL - only needed for local development when running frontend separately
# In production (combined Docker build), leave empty to use relative URLs
VITE_API_URL=http://localhost:8000

# Gemini API Configuration
VITE_GEMINI_API_KEY=your-gemini-api-key-here
VITE_GEMINI_MODEL=gemini-1.5-flash
